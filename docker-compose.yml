services:
  teabot:
    build: .
    container_name: teabot
    volumes:
      - ./data:/data
    entrypoint: ["sh", "-c", "python -u bot.py 2>&1 | tee -a ./bot_output.log"]
    restart: unless-stopped
